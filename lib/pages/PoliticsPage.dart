import 'package:flutter/material.dart';
import 'package:news/S_ArticleList.dart';

class PoliticsPage extends StatelessWidget {
  const PoliticsPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return SArticleList(
      
    );
  }
}