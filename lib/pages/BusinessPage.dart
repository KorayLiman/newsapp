import 'package:flutter/material.dart';
import 'package:news/ArticleList.dart';
class BusinessPage extends StatelessWidget {
  const BusinessPage({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return ArticleList(
      
    );
  }
}